server:
    port: 8083

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: microservices
  application:
    name: RATING-SERVICE


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true